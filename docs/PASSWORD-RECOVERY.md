# Password recovery
Enable two-step verification on the sender Gmail account and create a Google App Password.
Set SMTP_HOST, SMTP_PORT, SMTP_USER, SMTP_PASS and SMTP_FROM in .env for local use or .env.docker for Docker.
Never commit environment files. Test from the Forgot password link on the login page.