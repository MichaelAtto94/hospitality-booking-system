"use client";
import{FormEvent,useCallback,useEffect,useState}from"react";
type Guest={id:string;firstName:string;lastName:string;phone:string;email:string|null;nationality:string;nrcOrPassport:string|null;_count?:{bookings:number}};
export default function Guests(){const[rows,setRows]=useState<Guest[]>([]);const[msg,setMsg]=useState("");const load=useCallback(async()=>{const r=await fetch("/api/guests");if(r.ok)setRows(await r.json())},[]);useEffect(()=>{load()},[load]);async function save(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=e.currentTarget;const r=await fetch("/api/guests",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(new FormData(f)))});const d=await r.json();setMsg(r.ok?"Guest added successfully":d.message);if(r.ok){f.reset();load()}}const i="w-full rounded-xl border px-3 py-2.5";return <main className="p-5 md:p-8"><h1 className="text-3xl font-black">Guests</h1><p className="text-slate-500">Guest profiles and stay history.</p><form onSubmit={save} className="mt-6 grid gap-3 rounded-2xl border bg-white p-5 md:grid-cols-3"><input name="firstName" required placeholder="First name" className={i}/><input name="lastName" required placeholder="Last name" className={i}/><input name="phone" required placeholder="Phone" className={i}/><input name="email" type="email" placeholder="Email" className={i}/><input name="nrcOrPassport" placeholder="NRC or passport" className={i}/><input name="nationality" defaultValue="Zambian" required className={i}/><input name="address" placeholder="Address" className={`${i} md:col-span-2`}/><button className="rounded-xl bg-emerald-600 font-bold text-white">Add guest</button></form>{msg&&<p className="mt-3 text-sm text-emerald-700">{msg}</p>}<div className="mt-6 overflow-x-auto rounded-2xl border bg-white"><table className="w-full text-left text-sm"><thead className="bg-slate-50"><tr><th className="p-4">Guest</th><th>Phone</th><th>ID</th><th>Nationality</th><th>Bookings</th></tr></thead><tbody>{rows.map(g=><tr key={g.id} className="border-t"><td className="p-4 font-bold">{g.firstName} {g.lastName}</td><td>{g.phone}</td><td>{g.nrcOrPassport||"â€”"}</td><td>{g.nationality}</td><td>{g._count?.bookings??0}</td></tr>)}</tbody></table></div></main>}
