"use client";
import{FormEvent,useEffect,useState}from"react";
type Property={name:string;type:string;address:string;town:string;province:string;phone:string;email:string|null;tpin:string|null};
export default function SettingsPage(){const[property,setProperty]=useState<Property|null>(null);const[message,setMessage]=useState("");useEffect(()=>{fetch("/api/settings").then(r=>r.json()).then(setProperty)},[]);async function save(event:FormEvent<HTMLFormElement>){event.preventDefault();const response=await fetch("/api/settings",{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(new FormData(event.currentTarget)))});const result=await response.json();setMessage(response.ok?"Property settings updated":result.message);if(response.ok)setProperty(result)}if(!property)return <main className="p-8">Loading settings...</main>;const input="mt-1 w-full rounded-xl border border-slate-300 px-3 py-2.5";return <main className="p-5 md:p-8"><h1 className="text-3xl font-black">Property settings</h1><p className="text-slate-500">Official hotel or lodge profile.</p><form onSubmit={save} className="mt-6 grid max-w-4xl gap-4 rounded-2xl border bg-white p-6 md:grid-cols-2"><label>Property name<input name="name" defaultValue={property.name} required className={input}/></label><label>Type<select name="type" defaultValue={property.type} className={input}><option>LODGE</option><option>HOTEL</option><option>GUEST_HOUSE</option><option>RESORT</option></select></label><label className="md:col-span-2">Address<input name="address" defaultValue={property.address} required className={input}/></label><label>Town<input name="town" defaultValue={property.town} required className={input}/></label><label>Province<input name="province" defaultValue={property.province} required className={input}/></label><label>Phone<input name="phone" defaultValue={property.phone} required className={input}/></label><label>Email<input name="email" type="email" defaultValue={property.email??""} className={input}/></label><label>TPIN<input name="tpin" defaultValue={property.tpin??""} className={input}/></label><div className="md:col-span-2">{message&&<p className="mb-3 rounded-xl bg-emerald-50 p-3 text-sm text-emerald-800">{message}</p>}<button className="rounded-xl bg-emerald-600 px-5 py-3 font-bold text-white">Save settings</button></div></form></main>}
