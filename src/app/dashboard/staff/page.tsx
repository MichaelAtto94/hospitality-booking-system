"use client";
import{FormEvent,useCallback,useEffect,useState}from"react";
type Staff={id:string;firstName:string;lastName:string;email:string;phone:string|null;role:string;isActive:boolean};
const roles=["SUPER_ADMIN","MANAGER","RECEPTIONIST","ACCOUNTANT","HOUSEKEEPER"];
export default function StaffPage(){const[staff,setStaff]=useState<Staff[]>([]);const[message,setMessage]=useState("");const load=useCallback(async()=>{const response=await fetch("/api/staff");if(response.ok)setStaff(await response.json());else{const result=await response.json();setMessage(result.message)}},[]);useEffect(() => { const timer = setTimeout(() => { void load(); }, 0); return () => clearTimeout(timer); }, [load]);async function create(event:FormEvent<HTMLFormElement>){event.preventDefault();const form=event.currentTarget;const response=await fetch("/api/staff",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(Object.fromEntries(new FormData(form)))});const result=await response.json();setMessage(response.ok?"Staff account created":result.message);if(response.ok){form.reset();load()}}async function update(id:string,data:object){const response=await fetch(`/api/staff/${id}`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify(data)});const result=await response.json();setMessage(response.ok?"Staff account updated":result.message);if(response.ok)load()}const input="w-full rounded-xl border border-slate-300 px-3 py-2.5";return <main className="p-5 md:p-8"><h1 className="text-3xl font-black">Staff management</h1><p className="text-slate-500">Accounts, roles and access status.</p><form onSubmit={create} className="mt-6 grid gap-3 rounded-2xl border bg-white p-5 md:grid-cols-2 xl:grid-cols-3"><input name="firstName" required placeholder="First name" className={input}/><input name="lastName" required placeholder="Last name" className={input}/><input name="email" type="email" required placeholder="Email" className={input}/><input name="phone" placeholder="Phone" className={input}/><select name="role" className={input}>{roles.filter(r=>r!=="SUPER_ADMIN").map(role=><option key={role}>{role}</option>)}</select><input name="password" type="password" minLength={8} required placeholder="Temporary password" className={input}/><button className="rounded-xl bg-emerald-600 px-5 py-3 font-bold text-white md:col-span-2 xl:col-span-3">Create staff account</button></form>{message&&<p className="mt-4 rounded-xl bg-emerald-50 p-3 text-sm text-emerald-800">{message}</p>}<div className="mt-6 space-y-3">{staff.map(member=><article key={member.id} className="grid gap-4 rounded-2xl border bg-white p-5 md:grid-cols-[1fr_220px_auto] md:items-center"><div><p className="font-black">{member.firstName} {member.lastName}</p><p className="text-sm text-slate-500">{member.email} Â· {member.phone||"No phone"}</p></div><select value={member.role} onChange={event=>update(member.id,{role:event.target.value})} className={input}>{roles.map(role=><option key={role}>{role}</option>)}</select><button onClick={()=>update(member.id,{isActive:!member.isActive})} className={`rounded-xl px-4 py-2 text-sm font-bold ${member.isActive?"bg-red-50 text-red-700":"bg-emerald-50 text-emerald-700"}`}>{member.isActive?"Deactivate":"Activate"}</button></article>)}</div></main>}

