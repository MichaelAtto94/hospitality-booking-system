"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
export function BookingRequestActions({bookingId}:{bookingId:string}){const router=useRouter();const[loading,setLoading]=useState("");const[error,setError]=useState("");async function update(status:"CONFIRMED"|"CANCELLED"){setLoading(status);setError("");const response=await fetch(`/api/bookings/${bookingId}`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({status})});const result=await response.json();setLoading("");if(!response.ok)return setError(result.message??"Unable to update request");router.refresh()}return <div><div className="flex gap-2"><button disabled={!!loading} onClick={()=>update("CONFIRMED")} className="rounded-xl bg-emerald-600 px-4 py-2 text-sm font-bold text-white disabled:opacity-50">{loading==="CONFIRMED"?"Confirming...":"Confirm booking"}</button><button disabled={!!loading} onClick={()=>update("CANCELLED")} className="rounded-xl border border-red-200 bg-red-50 px-4 py-2 text-sm font-bold text-red-700 disabled:opacity-50">{loading==="CANCELLED"?"Declining...":"Decline"}</button></div>{error&&<p className="mt-2 text-xs text-red-700">{error}</p>}</div>}
