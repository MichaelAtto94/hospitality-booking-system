"use client";
import{useState}from"react";import{useRouter}from"next/navigation";
export function FrontDeskAction({id,status,label}:{id:string;status:"CHECKED_IN"|"CHECKED_OUT";label:string}){const router=useRouter();const[loading,setLoading]=useState(false);const[error,setError]=useState("");async function act(){setLoading(true);setError("");const response=await fetch(`/api/bookings/${id}`,{method:"PATCH",headers:{"Content-Type":"application/json"},body:JSON.stringify({status})});const result=await response.json();setLoading(false);if(!response.ok)return setError(result.message??"Update failed");router.refresh()}return <div><button disabled={loading} onClick={act} className="rounded-xl bg-emerald-600 px-4 py-2 text-sm font-bold text-white disabled:opacity-50">{loading?"Updating...":label}</button>{error&&<p className="mt-1 text-xs text-red-700">{error}</p>}</div>}
